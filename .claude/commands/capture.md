---
description: Run process capture (L1 extract → L3 replay → L2 process model) on engagement inputs.
argument-hint: "[file]"
---

Invoke the `aisa-capture` skill with the arguments provided.

Args: $ARGUMENTS
