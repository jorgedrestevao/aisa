---
description: Transition Framing → Options (generate 3-5 options incl. non-tech; the technology lens enters here).
argument-hint: "[--override <reason>]"
---

Invoke the `aisa-options` skill with the arguments provided.

(The `aisa-options` skill is implemented in a later build phase; until then this command reports that it is not yet available.)

Args: $ARGUMENTS
