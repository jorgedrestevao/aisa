---
description: Show engagement status (phase, round, SU summary, gaps). Use --check to validate the install.
argument-hint: "[--check]"
---

Invoke the `aisa-status` skill with the arguments provided.

Args: $ARGUMENTS
