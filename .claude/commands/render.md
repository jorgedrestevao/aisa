---
description: Render 1 or all 6 deliverables from the synthesis topic packs + decisions.
argument-hint: "[deliverable|--all]"
---

Invoke the `aisa-render` skill with the arguments provided.

(The `aisa-render` skill is implemented in a later build phase; until then this command reports that it is not yet available.)

Args: $ARGUMENTS
